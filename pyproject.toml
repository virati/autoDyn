[project]
name = "autodyn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fury>=0.12.0",
    "jax>=0.5.3",
    "matplotlib>=3.10.1",
    "networkx>=3.4.2",
    "numpy>=2.2.4",
    "scipy>=1.15.2",
]

[tool.uv.workspace]
members = [
    ".",
    ".",
    ".",
    ".",
]

[tool.uv.sources]
autodyn = { workspace = true, editable = true }

[dependency-groups]
dev = [
    "autodyn",
]
